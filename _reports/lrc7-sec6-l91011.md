# lrc7-sec6-l91011 — Lemma 9(ii), 10, 11 implementation report

Task: create `Research07/LRC7/Case5mL10.lean` proving `lemma9_ii`, `lemma10`,
`lemma11` (paper §6, |A|=5, m>1). Import only `Research07.LRC7.Case5mCases`.
Sibling agent owns `lemma9_i` (Case5mL9.lean) — do NOT touch it or any
existing file.

## Preliminary findings (resumed session)

### Statement fidelity — IMPORTANT

* `lemma11` as written in the spec (`enu7 = padicValNat` on the residue) is
  **FALSE** when two elements are congruent mod `7^{m+1}` (i.e. `eMod7 = 0`
  while `d_i ≠ d_j`). Counterexample m=1: `A1 = {1,8,57}` (all `runit7 = 1`,
  `e(8,57)=0`, `e(1,8)=e(1,57)=42` at ν=1). All 6 labelings fail —
  `padicValNat 7 0 = 0` reads the level-`≥m+1` pair as level 0.
  Brute check: 839/3000 sampled bad at m=1, 114/3000 at m=2.
* Fix (minimal, non-weakening): effective level
  `elevel7 m x y := if eMod7 m x y = 0 then m+1 else padicValNat 7 (eMod7 m x y)`
  = `min(ν(d_i−d_j), m+1)` — faithful to the paper's true 7-adic valuation.
  Brute check: 0/3000 bad at m=1,2 under `elevel` — the theorem holds.
* `lemma10` under `padicValNat` as written IS provable (0 bad in collision
  samples): a residue-0 pair `ν=0` is always the *smaller* side of the (i)
  inequality, so collisions route into case (i) automatically.
* `lemma9_ii`'s `h : ν(x)=ν(y)` with `padicValNat` is safe: `x=0 ↔ y=0`
  follows from `r(y)=j·r(x)`, j∈{2,3} — the all-zero edge is benign.

### lemma9_ii strategy (verified numerically, m≤3, 0 bad)

* `x = e(b1,b3)`, `y = e(b2,b3)` at common level h<m; `r(y)=j·r(x)`.
* j=2: `E := e(x,y) ≡ 2x−y` has `7^{h+1} | E`. If `E=0` → λ₁=1;
  `ν(E)=m` → scalar `c·E = 7^m` (via `exists_top_scalar_set`);
  `h<ν(E)<m` → `λ₁∈Λ_{ν(E)}` with `q(λ₁E)=0` (`exists_multLow_set_qdig`).
  Then `λ₂∈Λ_h` sets `q(λ₂x₁)=c_x` where `c_x = 0` for `ẽ∈{0,1}`, `6` for
  `ẽ=6` (and `ẽ∈{0,1}` forced when `E=7^m` — needs the pure-block borrow
  lemma `e=7^m → etd7 ∈{0,1}`). Get `q(λy)∈{0,6}`, `q(λ(x−e))∈{0,6}`.
* `q(λ(y−x))` handled via `z ≡ y−x ≡ x−e`: `q(z) ∈ q(x)−q(e)+{0,6}` (for
  `c_x=0`) or `q(z) ∈ q(y)−q(x)+{0,6}` (for `c_x=6`, gives −δ∈{0,6}).
* j=3: `ũ = 3q(y)+5q(x)` table: `λ∈Λ_h` sets `q(λy)=t=f(ũ)∈{0,5,6}`;
  finite check `q(λx)=qx+u∈{0,1,5,6}`, `q(λy)−q(λx)∈{0,1,6}`.
  `q(λ(y−x)) ∈ q(λy)−q(λx)+{0,6} ⊆ {0,1,5,6}`.
* Reversed pairs `e(b_j,b_i) = −e(b_i,b_j)`: `q(N−e) = 6−q(e)` for
  `7^m∤e` (level <m) — `{0,6}`-stable (j=2), `{0,1,5,6}`-stable (j=3).
* `remark8_i_int` (existing) + `remark8_i k=1` → `apLen ≤ 2` (j=2);
  need NEW `remark8_ii'` : diffs `{0,1,2,5,6} → apLen ≤ 3` (j=3) — NOTE the
  existing `remark8_ii` in Compress.lean uses hypothesis `x−y∈{0,1,5,6}`
  which under the ∀-pair quantification collapses to `{0,±1}`; the paper's
  `{0,±1,±2}` needs the `{0,1,2,5,6}` version — private decide.

### lemma10 structure

Case split: `∃` intersecting pairs with different ν → (i) (line-graph of
K_n connected); else uniform h on nonzero e's. For fixed l, `r(e_il)` is
injective in i unless `e_ij=0` (collision → back to (i)). With no
collisions: `{r(e_il),r(e_jl),r(e_kl)}` distinct nonzero, two in same
{1,2,4}/{3,5,6} part → `(x,2x,y)` → label per `y∈{3x,4x}` or `{5x,6x}`.

### lemma11 structure (elevel version)

Triangle ultrametric: `e13 ≡ e12+e23` ⇒ the two smallest pair-levels are
equal: levels `{h,h,K}`, `h≤K≤m+1`. `K>h` → (i). All `=h≤m` → r-count:
`|r|<6` gives `r(e13)=2r(e23)` (repeat is a `r_ij=r_jk` chain); `|r|=6`
gives `r(e13)=3r(e23)=±s` after relabel. All `=m+1` → all e=0 → (ii.1) via
`r=0`.

### Verification numbers (python, `_dev/check_*.py`)

* lemma9_ii j=2 strategy: m=1: 294 ok; m=2: 14700 ok; m=3: 971 sampled ok.
  `ẽ∈{0,1}` forced for `e=7^m` (t-set {0,1} for pure block, {0,1,6} total).
* lemma10: 4-sets 35+211876 ok (m=1,2); 5-sets 2000 ok; collisions 1546+219 ok.
* lemma11: distinct residues all ok; **padicValNat version fails 839/3000
  collisions at m=1**; elevel version 0 bad everywhere.

## Build log

### 2026-10-06 session 2 — dependency repair + helper layer GREEN

* The `.olean.private` read failure was a transient env issue (same tag
  `env-transient` as the prior Case5mL9 entry); retry succeeded.
* Helper layer (~240 lines) GREEN: `mul_pow_add_div`, `div_mod_pow_cast`,
  `qdig7_eMod_of`, `residue_decomp`, `natCast_sub_add`, `eMod7_zmod_cast`,
  `eMod7_congr`, `eMod7_self`, `eMod7_lt`, `eMod7_smul` (unit scalar),
  `qdig7_sub_resid` (weak membership version — to be replaced by L9-style
  exact borrow formula).

### Numerical verification of proof recipes (this session)

* j=2 recipe (`_dev/check_l9ii2d.py`): with the paper's λ₁ choices
  (`q(λ₁E)=0` for `h<ν(E)<m`, pure block `λE=7^m` for `ν(E)=m`, `λ₁=1` for
  `E=0`), the recipe `q(λx)=0` for `ẽ∈{0,1}` / `=6` for `ẽ=6` yields
  `{q(λx),q(λy),q(λz),q(λ(−z))} ⊆ {0,6}` in **0/1300+ sampled failures**
  at m=2,3,4.  Pure-block forces `ẽ∈{0,1}`.
  Borrow expressions used per branch (all unconditional in their branch):
  - `qe=0, ẽ∈{0,1}` (qx=0): `z ≡ x−E` → `qz = −δ' ∈ {0,6}`;
  - `qe=0, ẽ=6` (qx=6,qy=6): `z ≡ y−x` → `qz = −δ'' ∈ {0,6}`;
  - pure block `λE≡7^m` (qx=0): `z ≡ x−E` → `qz = 6` exactly.
* j=3 recipe (`_dev/check_l9ii3b.py` corrected): single `λ ∈ Λ_h`, `t=f(ũ)`
  table, `q(λx)=3ũ+5t ∈{0,1,5,6}`, `qy−qx=4ũ+3t ∈{0,1,6}` — deterministic,
  0 bad. (First version of the script tested the wrong difference set —
  pure Λ_h "failure" was a test artifact.)
* lemma10: spec form provable as written (collisions route to case (i)).
* lemma11: `elevel7` version required (countermodel documented above).

### Next

* Replace helper `qdig7_sub_resid` with L9-style exact borrow formula +
  `qdig_smul_eMod_eq` engine; add `remark8_ii'` (`{0,±1,±2}` diffs →
  `apLen ≤ 3`, private decide), `qdig7_neg`, `elevel7`.
* Then `lemma9_ii` (j=3 first — simpler), `lemma10`, `lemma11`.

### 2026-10-06 session 3 — expanded helper layer GREEN

* c5l10_12 build: 9 errors in expanded level-arithmetic layer; root causes:
  `Nat.mul_div_cancel_left` arg order (divisor first factor), `rw [hw_eq]`
  rewriting `w` inside its own RHS subterm `w / 7^(h+1)` (needed `conv_lhs`),
  `push_cast` normalizing `↑(7:ℕ)` to numeral `7` (needs `(7:ZMod 7)=0` via
  `decide`), `ZMod.natCast_eq_natCast_iff` (unprimed) yields `Nat.ModEq`
  not a `%`-equation — primed version required, `Nat.div_add_mod` gives
  `n*(m/n)` order, and **`wrap_sub_mod_gen` was FALSE** without
  `b ≤ a + c` (truncated nat subtraction; e.g. a=0,c=7,b=8: LHS 0 ≠ RHS 6).
* c5l10_13 build: **exit=0**, peak 8976MB, 33.9s — helper layer §0.1–§0.5
  complete: block lemmas (`nat_mod_pow_succ_of_padic`, `level_of_mod_block`,
  `add_mod_block`, `sub_mod_block`, `neg_resid_level`), congruence algebra
  (`eMod7_cast_add/sub`, `eMod7_add_eq`, `eMod7_sub_eq`, `eMod7_neg_eq`),
  level consequences (`enu7_neg`, `eMod7_level_gt_of_runit_eq`,
  `eMod7_level_of_runit_ne`, `eMod7_level_gt_of_runit_add`,
  `eMod7_level_of_runit_add`, `eMod7_level_min`), `elevel7` + 4 lemmas.
* Remaining: theorem layer lemma9_ii / lemma10 / lemma11, axiom printouts.

### 2026-10-07 session 4 — lemma9_ii COMPLETE, all gates GREEN

* `lemma9_ii` finished in `Research07/LRC7/Case5mL10.lean` (~line 1760–2500).
  Full theorem statement preserved (frozen spec). Structure: degenerate
  `x=0` case via `λ=1`; `j=2` via `E=eMod7 m x y` effective-level split
  (`E=0` / `hν<ν(E)`), `λ₁` zeroing `q(λ₁E)` or pure-block `λ₁E≡7^m`,
  `etd7∈{0,1,6}` via `qdig_eMod_sub_etd7`/`etd7_of_pure_block`, `cx∈{0,6}`
  correction, `λ₂∈Λ_h` with `q(λ₂λ₁x)=cx`, `λ=λ₂λ₁`, `remark8_i` on
  `{0,1,6}` etd bounds; `j=3` via `lemma9_ii_table3` finite `t∈{0,5,6}`,
  single `Λ_h` multiplier `λ=1+k·7^{m−hν}`, `r(y)=3r(x) ⇒ 3⁻¹=5` gives
  `q(λx)=qx+2qy+5t`, `remark8_ii'_dec`/`remark8_ii_int` difference
  compression to `apLen≤3`.
* Fixes this session: `hν1y` needed explicit `exact hyν` after
  `padicValNat_mul_seven` (ν(y)=hν not rfl since hν:=ν(x)); `hBLsame`
  needed `runit7_mul lam u'`/`runit7_mul lam v'` explicit args (pattern
  `r(?a*?b)` was let-unfolding `r(lam)`); `key`'s `rcases ... with rfl`
  substituted `b1/b2/b3` away → replaced with `rcases ... hu'` + `rw [hu',hv']`;
  `residN_multLow7` needed `(k:=k)`; j=3 `ring` doesn't reduce `−5≡2` in
  ZMod 7 → explicit `5*(t−qy)=5t+2qy` via `(−5:ZMod 7)=2` by decide;
  `hlowx/hlowy` used `rw [hlamdef]` (not `←`).
* Gates: `lake build` 8933 jobs clean; `#print axioms` lemma9_ii/lemma10/
  lemma11 all `[propext, Classical.choice, Quot.sound]`; zero
  sorry/admit/native_decide/unsafe; `Case5mL9.lean` untouched.
* Environmental: repeated transient `failed to read file '*.olean(.private)'`
  (Defender/4.2GB-free memory pressure) — non-deterministic, resolves on
  retry; direct `lake env lean` compiles and `lake build` both green.
